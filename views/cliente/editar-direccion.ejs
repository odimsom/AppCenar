<div class="p-6 text-white">
  <div class="flex justify-between items-center mb-6">
    <h1 class="text-3xl font-bold text-petrel">Editar dirección</h1>
    <a
      href="/cliente/direcciones"
      class="bg-gray-700 hover:bg-gray-600 text-white font-semibold px-4 py-2 rounded-lg flex items-center gap-2 transition"
    >
      <i class="fa fa-arrow-left"></i> Volver
    </a>
  </div>

  <div class="flex justify-center">
    <div class="w-full max-w-2xl bg-benthic rounded-lg p-6 shadow">
      <form
        action="/cliente/direcciones/editar/<%= direccion.id %>"
        method="POST"
        class="space-y-4"
      >
        <div>
          <label
            for="nombre"
            class="block mb-1 font-semibold text-white"
            >Nombre de la dirección</label
          >
          <input
            type="text"
            id="nombre"
            name="nombre"
            required
            value="<%= direccion.nombre %>"
            class="w-full px-4 py-2 rounded-lg bg-gray-800 text-white border border-gray-700 focus:outline-none focus:ring-2 focus:ring-petrel transition"
          />
        </div>

        <div>
          <label
            for="descripcion"
            class="block mb-1 font-semibold text-white"
            >Dirección completa</label
          >
          <textarea
            id="descripcion"
            name="descripcion"
            rows="4"
            required
            class="w-full px-4 py-2 rounded-lg bg-gray-800 text-white border border-gray-700 focus:outline-none focus:ring-2 focus:ring-petrel transition"
          >
<%= direccion.descripcion %></textarea
          >
        </div>

        <div class="flex justify-end">
          <button
            type="submit"
            class="bg-petrel hover:bg-bluedart text-white font-bold py-2 px-6 rounded-lg transition"
          >
            <i class="fa fa-save mr-2"></i> Actualizar dirección
          </button>
        </div>
      </form>
    </div>
  </div>
</div>
